{
  "name": "my-checker-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "devDependencies": {
    "prisma": "^6.2.0"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "@slack/web-api": "^7.8.0",
    "prisma": "^6.2.0"
  },
  "scripts": {
    "dev": "node app/api/server.ts",
    "build": "prisma generate && vercel build",
    "start": "node app/api/server.ts",
    "postinstall": "prisma generate"
  }
}
